#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    /* sum 3 numbers */
    /* constraints 1 < a , b , c < 1000 */
    int a, b, c;
    scanf("%d %d %d", &a, &b, &c);
    printf("%d", a + b + c);
    return 0;
}
